import java.io.IOException;
import java.util.ArrayList;
public abstract class Filter {
    DataInput dataInput = new DataInput();
    ArrayList<Double> data;
    {
        try {
            data = dataInput.fileReader();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
    int size = data.size();
    ArrayList<Double> medfilter = new ArrayList<>();
    ArrayList<Double> SecondMedList = new ArrayList<>();
    int window = Median.Window();
    double sum = 0;
    int FirstountingElement = 0;
    int SecondCountingElement = window;
    double value = 0;
    int supportValue1;
    int supportValue2;

    public abstract void calc();
}
